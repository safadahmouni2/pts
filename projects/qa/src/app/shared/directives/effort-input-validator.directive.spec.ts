import { EffortInputValidatorDirective } from './effort-input-validator.directive';

describe('EffortInputValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EffortInputValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
