import { Product } from "./product.model";
import { Topic } from "./topic.model";

export class DefaultTopic {
    id: number;
    topicId: number;
    productId: number;
   

    constructor() {
    }
}